apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.+'
}

version = '0.2-SNAPSHOT'

jar {
    manifest {
        attributes 'Implementation-Title': 'Printable',
                'Implementation-Version': version
    }
}
